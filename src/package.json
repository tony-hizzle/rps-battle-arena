{
  "name": "rps-battle-arena-backend",
  "version": "1.0.0",
  "description": "Backend Lambda functions for RPS Battle Arena",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf dist",
    "compile": "tsc"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.2",
    "@types/uuid": "^10.0.0",
    "jest": "^29.7.0",
    "typescript": "^5.7.2"
  }
}